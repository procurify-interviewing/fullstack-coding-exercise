# Purchase Requests - interview exercise

A small Django + React project. It runs, but the API endpoint is a stub and the React app is a
shell. The brief is in `EXERCISE.md`.

## Prerequisites

- Python 3.10 or newer (`python3 --version`). SQLite ships with Python.
- Node 20 or newer (`node --version`).
- No Docker, no database server.

## Setup after a fresh clone

macOS / Linux:

```bash
./setup.sh
```

Windows (PowerShell):

```powershell
.\setup.ps1
```

Either script creates `backend/.venv`, installs the Python dependencies, runs migrations, seeds
sample data, runs the tests, and installs the frontend's npm dependencies. It takes a couple of
minutes on a normal connection.

If you prefer to do it by hand:

```bash
cd backend
python3 -m venv .venv
source .venv/bin/activate          # Windows: .\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
cd src
python manage.py migrate
python manage.py seed_data
python manage.py test
cd ../../frontend
npm install
```

## Run

```bash
make dev
```

That starts both servers in one terminal; Ctrl-C stops both.

| | |
| --- | --- |
| Frontend | <http://localhost:5173> |
| API | <http://localhost:8000/api/purchase-requests/> |

Open the frontend and you should see a `501 Not Implemented` response rendered on the page.
That is the Django stub, reached through the dev server. The React app calls relative
`/api/...` paths and Vite proxies them to Django, so there is no CORS to configure and no API
base URL to set.

To run one half on its own: `make backend` or `make frontend`.

## Tests

```bash
make test
```

`backend/src/tests/test_purchase_request_api.py` fails out of the box - it is a stub for you to
write. Everything else passes. Tests are pytest test cases; see `backend/README.md`.

## Sample data

`make seed` resets and re-creates the dataset. It is safe to run as many times as you like.
Users after a fresh seed:

| username | id |
| --- | --- |
| alice | 1 |
| bob | 2 |
| carol | 3 |

## Repo tour

```
backend/
  README.md                 backend detail: Django app, SQLite database
  requirements.txt          Python dependencies
  pytest.ini                pytest config
  db.sqlite3                generated by migrate + seed_data
  src/
    manage.py
    config/                 settings, root urls
    purchase_requests/
      models.py             PurchaseRequest, Approval   (provided)
      views.py              list_purchase_requests      (stub, start here)
      urls.py               GET /api/purchase-requests/ (wired)
      management/commands/
        seed_data.py        deterministic sample data   (provided)
    tests/
      factories.py          helpers for building test data
      conftest.py           fixtures for the API tests
      test_project.py       sanity checks           (provided, pass)
      test_purchase_request_api.py
                            API tests               (stub, fails on purpose)
frontend/
  package.json
  vite.config.ts            dev server, /api proxy to Django
  index.html
  src/
    main.tsx                React entry point
    App.tsx                 the app                     (shell, start here)
    index.css
EXERCISE.md                 the brief
setup.sh / setup.ps1        one-shot setup
Makefile                    dev / backend / frontend / migrate / seed / test
```

Django REST Framework is installed. Using it is optional; plain Django views are fine.

You may add files and packages. If you want to change the models, say so first.

## Troubleshooting

- `python: command not found` - use `python3`, or run `./setup.sh` which finds the right interpreter.
- `No module named django` - the virtualenv is not active. Run the activate line above, or use the `make` targets, which do not need it.
- `npm: command not found` - install Node 20 or newer from <https://nodejs.org/>.
- `Port 8000 is already in use` - `python manage.py runserver 8001`, and point `vite.config.ts`'s proxy target at the same port.
- `Port 5173 is already in use` - `npm run dev --prefix frontend -- --port 5174`.
- PowerShell refuses to run `setup.ps1` - run `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` first, then retry.
- Want a clean slate - delete `backend/db.sqlite3` and re-run `make migrate seed`.
